# Zero-cost model-modality probe: boots the tree, lists every provider's
# NOTE: run through scripts/run-host-probes.mjs (substitutes __WORKSPACE__); direct copy-paste needs the token replaced.
# models with declared inputModalities, exits without any model request.
- id: headless-runner
  disabled: true
- insert:
    - id: probe-model-capabilities
      name: file://__WORKSPACE__/scripts/probe-model-capabilities.js
