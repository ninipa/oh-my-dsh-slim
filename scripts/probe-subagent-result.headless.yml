# Zero-cost REAL-service probe for subagent-result.js (registered tool +
# NOTE: run through scripts/run-host-probes.mjs (substitutes __WORKSPACE__); direct copy-paste needs the token replaced.
# real sessionQuery over live/cold children). Run through
# scripts/run-host-probes.mjs; __WORKSPACE__ is substituted.
- insert:
    - id: agent-presets
      name: '@deepseek-ai/dsh-agent-presets'
      config:
        default: oh-my-dsh-slim
- id: headless-runner
  disabled: true
- insert:
    - id: probe-subagent-result
      name: file://__WORKSPACE__/scripts/probe-subagent-result.js
