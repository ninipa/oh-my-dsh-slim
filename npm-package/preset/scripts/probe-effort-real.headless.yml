# Zero-cost REAL-event-chain probe for effort-by-role.js (agent/request
# NOTE: run through scripts/run-host-probes.mjs (substitutes __WORKSPACE__); direct copy-paste needs the token replaced.
# waterfall over the real dispatch bus; phases via OMDS_EFFORT_PHASE).
# Run through scripts/run-host-probes.mjs; __WORKSPACE__ is substituted.
- insert:
    - id: agent-presets
      name: '@deepseek-ai/dsh-agent-presets'
      config:
        default: oh-my-dsh-slim
- id: headless-runner
  disabled: true
- insert:
    - id: probe-effort-real
      name: file://__WORKSPACE__/scripts/probe-effort-real.js
